/****
 * Create an example of JavaScript Singleton
 * After the first object is created, it will return additional references to itself
 */

let obj = (function (){
    let objInstance;
    function create(){

    }
    return {

    };
})();

//let obj1 = obj.getInstance();
//let obj2 = obj.getInstance();

//what is a javascript singleton? 
//an object, where everytime you try to get an instance of, you get reference to the same instance
